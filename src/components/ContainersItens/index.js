import React from "react";

import { ContainerItens as Container } from "../../containers/Home/styles";

function ContainerItens({children}){

    return <Container>{children}</Container>
}

export default ContainerItens